// It is an array with specific length and specify the type of each element.

 let data : [String, number , boolean] = ["Haile", 23, true]

 data.push("Ethiopia"); // This will cause an error because the tuple is fixed length and types

